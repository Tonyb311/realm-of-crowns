# =============================================================================
# Realm of Crowns - Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values.
# For local Docker development, the defaults below work out of the box.

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
# Local Docker:
DATABASE_URL="postgresql://roc_user:roc_password@localhost:5432/realm_of_crowns"
# Azure example:
# DATABASE_URL="postgresql://rocadmin:<PASSWORD>@<SERVER>.postgres.database.azure.com:5432/realm_of_crowns?sslmode=require"

# -----------------------------------------------------------------------------
# Cache (Redis)
# -----------------------------------------------------------------------------
# Local Docker:
REDIS_URL="redis://localhost:6379"
# Azure example:
# REDIS_URL="rediss://:<PRIMARY_KEY>@<REDIS_HOSTNAME>:6380"

# -----------------------------------------------------------------------------
# Authentication
# -----------------------------------------------------------------------------
# IMPORTANT: Change this to a long random string in production
JWT_SECRET="change-this-to-a-random-secret-in-production"
JWT_EXPIRES_IN="7d"

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
PORT=4000
NODE_ENV=development

# -----------------------------------------------------------------------------
# Client
# -----------------------------------------------------------------------------
# URL where the client is running (used for CORS)
CLIENT_URL="http://localhost:3000"
